#!/usr/bin/env node

/**
 * Dependencies.
 */

var program = require('commander');
var commit = require('..');

/**
 * Program.
 */

program
  .version('0.0.1')
  //.option('-p, --push', 'Push to remote repository after generating commits')
  .parse(process.argv);

/**
 * Run.
 */

commit(program);